overwrite: true
schema:
  - "http://localhost:8000/graphql"
  - "http://localhost:4000/"
documents: "src/**/*.{ts,tsx}"
generates:
  ./src/gql/:
    preset: client
    config:
      withHooks: true
      enumsAsTypes: true
      futureProofEnums: true
ignoreNoDocuments: true
