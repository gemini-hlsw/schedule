# schedule

GPP Schedule is the app that allows QCs to interact with the (Automated Scheduler)[https://github.com/gemini-hlsw/scheduler]

## To run development
We are using NodeJS 19 and yarn for development.

```bash
$ yarn run dev
```